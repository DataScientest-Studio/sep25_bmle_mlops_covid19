# Copier vers secrets.yaml et remplir avec tes vraies valeurs.
# Ne jamais commiter secrets.yaml (il est dans .gitignore).

# --- Supabase (obligatoire pour le feedback) ---
# Dans Supabase : Project Settings > API
#   - Project URL → mettre l’URL REST : https://<project_ref>.supabase.co/rest/v1
#   - Project API keys → utiliser "anon" (publique) ou "service_role" (côté serveur, plus de droits)
database:
  host: "https://XXXXXXXX.supabase.co/rest/v1"
  password: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# Optionnel : port, name, user (non utilisés par l’API feedback)
#  port: 5432
#  name: postgres
#  user: postgres

# --- S3 (optionnel, pour stocker les images des feedbacks) ---
# Si absent, le feedback est quand même enregistré en base sans image_url.
# S3:
#   bucket_name: "mon-bucket"
#   access_key: "0042..."
#   secret_key: "K004..."
